    <form method="post" enctype="multipart/form-data" asp-controller="Home" asp-action="GetFile2">
        <div class="form-group">
            <div class="col-md-10">
                <p>فایل را برای آماده سازی برای ترجمه اینجا آپلود کنید </p>
                <input class="btn btn-success" type="file" name="files" multiple>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <input class="btn btn-success" type="submit" value="Download">
            </div>
        </div>
    </form>
    <form method="post" enctype="multipart/form-data" asp-controller="Home" asp-action="GetFile3">
        <div class="form-group">
            <div class="col-md-10">
                <p>فایل اصلی و ترجمه شده را قرار دهید </p>
                <input class="btn btn-success" type="file" name="files" multiple>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <input class="btn btn-success" type="submit" value="Download">
            </div>
        </div>
    </form>

@section Scripts
{ 
    @*<script>
    var formData = new FormData();
formData.append('file', $('#file')[0].files[0]);

$.ajax({
       url : 'upload.php',
       type : 'POST',
       data : formData,
       processData: false,  // tell jQuery not to process the data
       contentType: false,  // tell jQuery not to set contentType
       success : function(data) {
           console.log(data);
           alert(data);
       }
});
    </script>*@
}
